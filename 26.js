'use strict';
const usdCurr = 28;
const discount = 0.9;
function convert (amount, curr){
    return curr * amount;
}

function promoting (result){
    console.log (result * discount);
    return
}

const res = convert(500, usdCurr);
promoting(res);
